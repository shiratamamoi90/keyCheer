# 仕様設計のルール(決定的部分)

## 何を仕様化するか

- **仕様化する**:決定的な振る舞い。コアロジック、状態遷移、入出力契約、エラー処理、境界条件。
- **仕様化しない**:非決定的な方針(探索・チューニング)。→ experiments/ の hypothesis に書く。

## 書式:GIVEN / WHEN / THEN

- 1要件 = 1見出し。1シナリオ = 後でテスト1メソッドに対応させる。
- シナリオ名は test メソッド名に流用できる短さにする。

## 必須カバレッジ

- 正常系・異常系・境界(0件、空、上限、同時、リソース枯渇 等)を必ず網羅する。
- 外部効果のあるものは「発動条件」「効果」「失敗時」を分けて書く。
- 曖昧さが残る箇所は `[要確認]` を付け、人間の確定を待つ。勝手に決めない。

## 確定の流れ(人間の承認点)

1. `proposal.md`(なぜ変えるか / ADDED・MODIFIED の影響範囲)を書く。
2. `spec.md`(GIVEN/WHEN/THEN)を書く。
3. **人間が spec を承認するまで実装に進まない。** ここが唯一 AI に丸投げしない工程。

## spec.md テンプレ

```markdown
# 要件: <名前>

<1文で何を保証するか>

## シナリオ: <正常系の名前>

- GIVEN <前提>
- WHEN <操作>
- THEN <期待される結果>

## シナリオ: <異常系の名前>

- GIVEN ...
- WHEN ...
- THEN ...

## シナリオ: <境界の名前> [境界]

- GIVEN ...
- WHEN ...
- THEN ...
```
