# 確定要件(source of truth)

**決定済み論点を統合した結果だけを置く。** 議論の途中は `decisions/undecided/` にあり、
決まったものを `decisions/decided/` から**ここへ書き起こす**(手順は
`.claude/rules/doc-rules.md`「確定の流れ」、実行は `/decide`)。

- 書くのは**何を満たすか**だけ。**どう作ったか(層・ファイル分割・関数名)は書かない** —
  ソースツリーを見れば分かる。なぜそう作ったかは `decisions/` にある。
- シナリオ ID の先頭4桁は**論点番号**と一致させる(`.claude/rules/impl-rules.md` N-2)。
- ここに書いたシナリオは `scripts/validate.sh` が `tests/` と双方向で照合する。
  ID があって対応するテストが無ければ「未実装」として NG になる。

## 要件ファイルと対応する論点

| 要件 | 中身 | 決めた論点(ID の先頭4桁) |
| --- | --- | --- |
| `key-counter.md` | キー押下のカウントとアクティブ判定 | 0011 |
| `speed-zone.md` | KPM 算出(60 秒固定窓)と速度ゾーン分類 | 0012 |
| `cheer-trigger.md` | 発動条件・プールからの選択・フォールバック | 0013 |
| `time-of-day.md` | 時(0..23)→ 時間帯4値の分類境界 | 0014 |
| `data-model.md` | 設定 / プール / 統計 / ランタイムのデータ契約 | 0015 |
| `integrations.md` | 生成プロバイダー(ローカル / 外部 API)の契約 | 0016 |
| `popup.md` | 応援ポップアップの表示内容・時間・上書き・音声再生 | 0017 |
| `main-window.md` | メインウィンドウの開閉ライフサイクル | 0018 |
| `character-creation.md` | キャラ作成フォームの入力・検証・保存 | 0019 |
| `pool-generation.md` | プール・wav の生成と永続化、生成物での発動 | 0020 |
| `glossary.md` | ドメイン語彙の「正」(N-1。`validate.sh` が参照) | — |

## 全要件に共通する不変条件

実装側の不変条件(依存方向・発動経路の分離など)の正本は `.claude/rules/impl-rules.md`。
ここに置くのは**要件として満たすべきこと**だけ。

- **プライバシー**: キーの種類のみ数える。入力内容は保存も送信もしない。
  外部送信はキャラ作成時に明示同意した場合のみ(`.claude/rules/security-rules.md`)。
- **フォールバック**: 外部依存(Ollama / VOICEVOX / sd.cpp)が無くてもクラッシュせず、
  同梱 `baseline` 定型文で応援する。
- **再現性**: 乱数はシード固定可能。時刻・乱数は引数で受け取る。
